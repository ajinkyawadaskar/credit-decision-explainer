.venv/
__pycache__/
*.pyc
.env
.DS_Store
data/*.csv
data/*.data
data/*.zip
# models/xgb_credit.json is deliberately TRACKED — Railway builds from the repo
# and there is no training step at deploy time, so the artifact must ship with it.
models/*.pkl
.ipynb_checkpoints/
.deepeval*

# Working config for my AI tooling. Kept locally, not part of the deliverable.
CLAUDE.md
